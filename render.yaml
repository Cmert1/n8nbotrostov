services:
  - type: web
    name: n8n-service
    runtime: docker
    plan: free
    envVars:
      - key: N8N_ENCRYPTION_KEY
        generateValue: true
      - key: NODE_OPTIONS
        value: --max-old-space-size=350
